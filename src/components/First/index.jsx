import CenterItems from '../CenterItems'

import './styles.css'

function First({ color }) {
  return <section className="first">
    <CenterItems text="Rosa Neon" />
  </section>
}

export default First;